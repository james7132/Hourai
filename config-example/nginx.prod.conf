worker_processes     5;
worker_rlimit_nofile 8192;
error_log /dev/stdout info;

events {
  worker_connections  4096;
}

http {
  include mime.types;
  access_log /dev/stdout;

  include /etc/letsencrypt/common/ip_whitelist.conf;

  server {
    listen 80;
    listen [::]:80;
    server_name hourai.gg;
    return 301 https://hourai.gg$request_uri;
  }

  server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name hourai.gg;

    include /etc/letsencrypt/common/common_prod.conf;
    include /etc/nginx/common/ssl_certs.conf;
    include /etc/letsencrypt/common/hourai_gg_common.conf;
  }

  server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name docs.hourai.gg;
    root /var/www/docs;

    include /etc/letsencrypt/common/common_prod.conf;
    include /etc/nginx/common/ssl_certs.conf;

    add_header Cache-Control no-cache;

    location / {
      try_files $uri $uri/ /index.html;
    }
  }

  upstream api {
    server hourai:8080 fail_timeout=0;
  }
}
